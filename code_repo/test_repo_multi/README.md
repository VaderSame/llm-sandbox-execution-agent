# Multi-File Test Repository

This is a test repository to verify that the Execution Agent can properly explore a multi-file project and resolve dependencies between files (like `utils.py`).

## Usage

To run this repository, you must use the `main.py` entry point. It requires a name parameter. 

Run the following command:
`python main.py --name "LangGraphAgent" --count 3`
